{
  "name": "mylg-files-service",
  "version": "1.0.0",
  "description": "File lifecycle management service for MYLG",
  "type": "module",
  "scripts": {
    "deploy:dev": "serverless deploy --stage dev",
    "deploy:prod": "serverless deploy --stage prod",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "vitest": "^1.x"
  }
}
